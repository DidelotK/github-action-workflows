name: Apply on PR
on:
  pull_request:
    types:
      - synchronize
      - opened
      - reopened
jobs:
  plan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: [sandbox, prod]
        layer: [10_base, 20_eks_core_services]
    environment: ${{ matrix.env }}_${{ matrix.layer }}
    steps:
      - name: Apply
        run: echo "Applyyy"
